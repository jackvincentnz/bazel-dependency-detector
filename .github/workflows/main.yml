on: [push]

jobs:
  submit-bazel-dependencies:
    name: Submit Bazel Dependencies
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Bazel Dependency Submission Action
        uses: ./
